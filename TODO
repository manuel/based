Handle overflow when writing to log. also check before writing whether
   too large for a single file.

Don't include URL fragment after # in ID.

test suite

Move httpd into own, untrusted process.

content-md5 end-to-end integrity check

robust file format, tolerant log redo

replication

Limit path components' lengths and number of components to something
sensible.

Allow '/' in ID.  Parse URI before decoding... ? and / in path
segments should be allowed.
